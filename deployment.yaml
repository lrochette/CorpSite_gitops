apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: globex
  name: globex
spec:
  selector:
    matchLabels:
      app: globex
  template:
    metadata:
      labels:
        app: globex
    spec:
      containers:
        - name: globex
          image: lrochette/globex:8741fbc613e9e8b66f299f5568f938ac7030491c
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
